export interface Experience {
  company: string;
  position: string;
  period: string;
  description: string;
  achievements: string[];
  skills: string[];
}

export interface ExperienceContent {
  title: string;
  subtitle: string;
  keyAchievements: string;
}
